{
  "name": "mesh-maker-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter mesh-maker-demo dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
